#Tuning Parameters Analysis of Autoplot

#EN plots
autoen1 shows RMSE improving (dropping from ~76 to ~70) as regularization increases, 
then flattening out, with the lasso proportion having little effect. autoen2 shows 
the opposite pattern, starting at a better RMSE of ~66 with low regularization 
and worsening as regularization increases, making the 2nd recipe the overall 
better-performing recipe.

